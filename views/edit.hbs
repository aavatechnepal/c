<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/CSS/createChart.css">
    <title>createChart</title>
</head>

<body>

    <style>

    @import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,300;0,400;0,500;0,700;1,500&display=swap');

*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Roboto', sans-serif;
}

:root{
    --primary-color : #34BA82;
    --text-color : #011A41;
}

.container{
    max-width: 1260px;
    margin-inline: auto;
    padding-inline: 20px;
}
        .updateBtn{
            padding: 12px 34px;
            border: none;
            border-radius: 7px;
            background-color: var(--primary-color);
            color: white;
        }

        .updateBtn:hover{
            transition: 1s;
            background-color: var(--text-color);
            box-shadow: 2px 3px 3px rgba(0, 0, 0, 0.527);
        }

    </style>

    <section class="container searchbar">
        <input type="text" name="search" id="" placeholder="Search"> 

    </section>



    <section class="createChart container">     

        <div class="">     
        <h3> Create chart of Accounts</h3>  
          

            <form action="/update/{{editData._id}}" method="post">
                <input type="text" name="description" value="{{editData.description}}" id="" placeholder="Description">
                <input type="text" name="account1" value="{{editData.account1}}" id="" placeholder="Account1">
                <input type="text" name="account2" value="{{editData.account2}}" id="" placeholder="Account2">               
                <button class="updateBtn" type="submit">update</button>

            <p class="msg">{{message}}</p>
            <p class="error">{{error}}</p>
            <a href="/listchart">See the chart of Account list</a>

            </form>
              </div>
            
         

    
    </section>

</body>

</html>